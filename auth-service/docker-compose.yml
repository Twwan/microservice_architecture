services:
  auth-service:
    container_name: auth-service
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "npm run start"
    env_file:
      - ../.env
    ports:
      - "3002:3002"
    networks:
      - lab2-net

networks:
  lab2-net:
    external: true
